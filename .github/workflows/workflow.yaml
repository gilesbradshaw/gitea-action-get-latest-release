---
name: Test
on: [push]

jobs:
  build:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: npm i
      - run: pwd
      - run: ls
      - uses: gilesbradshaw/gitea-action-get-latest-release@${{ github.ref_name }}
        with:
          repository: gitea/tea
          serverUrl: https://gitea.com/
      - uses: gilesbradshaw/gitea-action-get-latest-release@${{ github.ref_name }}
        with:
          repository: gitea/tea
          serverUrl: https://gitea.com/
          excludes: preRelease, draft
      - uses: gilesbradshaw/gitea-action-get-latest-release@${{ github.ref_name }}
        with:
          repository: gitea/tea
          serverUrl: https://gitea.com/
          token: ${{ github.token }}
